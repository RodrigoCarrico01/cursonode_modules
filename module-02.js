const module01 = require("./module-01") 
//quando vamos importar um arquivo como módulo para colocá-lo dentro de outro devemos nos lembrar de armazenar o arquivo  em alguma referência se não ele traz o arquivo todo e não por módulos


//a executar um módulo importado
module01.executa()

module01.oculta()
